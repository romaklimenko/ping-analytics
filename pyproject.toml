[project]
name = "ping-analytics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "azure-storage-blob>=12.28.0",
    "duckdb>=1.4.4",
    "invoke>=2.2.1",
    "python-dotenv>=1.2.2",
]
